# =============================================================================
# Eigenvue Python Package — Development Environment Variables
#
# These are only used during development and testing.
# The published package has no environment variable dependencies.
# =============================================================================

# --- Local Server ---
# Port for the local visualization server (eigenvue.show()).
# Default: random available port. Set this to use a fixed port.
EIGENVUE_SERVER_PORT=

# --- Debugging ---
# Set to "1" to enable verbose logging in generators.
EIGENVUE_DEBUG=

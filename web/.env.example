# =============================================================================
# Eigenvue Web Application — Environment Variables
#
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# In CI/CD, these are set via GitHub Actions environment variables.
# In Firebase Hosting, they are baked into the static build at build time.
# =============================================================================

# --- Site URL ---
# Used for canonical URLs, OG tags, and sitemap generation.
# MUST be the full URL including protocol, without trailing slash.
NEXT_PUBLIC_SITE_URL=https://eigenvue.web.app

# --- Analytics (optional) ---
# Set to enable privacy-respecting analytics. Leave empty to disable.
# Plausible: set the domain (e.g., "eigenvue.web.app")
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# Umami (alternative to Plausible):
NEXT_PUBLIC_UMAMI_WEBSITE_ID=
NEXT_PUBLIC_UMAMI_SCRIPT_URL=

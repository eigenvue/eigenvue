{
  "description": "2-2-1 XOR-style network with sigmoid + MSE, seed 'backprop-xor'",
  "inputs": {
    "inputValues": [1, 0],
    "targets": [1],
    "layerSizes": [2, 2, 1],
    "activationFunction": "sigmoid",
    "lossFunction": "mse",
    "learningRate": 0.5,
    "seed": "backprop-xor"
  },
  "expectedStepCount": 9,
  "keyStates": [
    { "stepId": "forward-start", "state": { "layerSizes": [2, 2, 1] } }
  ],
  "notes": "9 steps: forward-start + 2 forward layers + compute-loss + backward-output + backward-hidden-1 + update-weights-1 + update-weights-2 + complete."
}

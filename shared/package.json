{
  "name": "@eigenvue/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared type definitions and schemas for the Eigenvue platform",
  "main": "types/index.ts",
  "types": "types/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "vitest": "^1.0.0"
  }
}
